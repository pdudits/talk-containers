apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "container-app.fullname" . }}-test-connection"
  labels:
    {{- include "container-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "container-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
